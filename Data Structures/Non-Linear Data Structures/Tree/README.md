# Tree Data Structure

- Tree is a recursive data structure.
- Nodes of a tree maintains a parent-child relationship.
- A node can have many number of children nodes but it can have only one single parent. 
